// Minimal demo entrypoint - prints instructions and verifies packages exist

console.log('AgentGate demo (placeholder)');
console.log('- To run provider server: (from repo root) npm --workspace @agentgate/server run build && npm --workspace @agentgate/server run start');
console.log('- To build the SDK: npm --workspace @agentgate/sdk run build');
console.log('This demo will be expanded in Task 4 to include provider and consumer scripts.');
